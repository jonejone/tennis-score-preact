import {Fragment, FunctionalComponent, h } from 'preact';
import {MatchesByDate} from "../../components/matches_by_date/index"


const Home: FunctionalComponent = () => {

    return (
        <MatchesByDate />
    );
};

export default Home;
